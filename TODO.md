# TODO

- [ ] Inspect where AI Explorer Arena is routed/embedded (likely App page/dashboard/worlds)
- [ ] Make Arena1RoomPage responsive (layout + mobile scrolling + scaling)
- [ ] Add small-screen breakpoint behavior (collapse 3-column to single column, reduce transforms/perspective)
- [ ] Ensure banner/back button/header don’t overlap on small screens
- [ ] Run typecheck/lint/build (or at least `next build`) to ensure no TS errors

